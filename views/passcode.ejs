<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Enter Passcode</title>
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@4.3.0/fonts/remixicon.css"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="h-screen w-full px-20 font-[Courier New] select-none flex flex-col">
      <nav class="flex justify-between items-center py-6">
        <h1 class="font-semibold text-2xl">Khatabook</h1>
        <div class="n-right font-semibold flex items-center gap-6">
          <a class="hover:text-blue-500" href="#">Forgot Passcode?</a>
        </div>
      </nav>
      <div class="flex-grow flex flex-col items-center">
        <h1 class="py-6 mt-20 font-semibold text-2xl">Enter Passcode</h1>
        <form action="/verify-passcode/<%= hisaab._id %>" method="post" class="flex flex-col gap-3">
          <input type="password" name="passcode" placeholder="Passcode" class="bg-zinc-50 border-2 border-zinc-200 px-3 py-2 rounded-md outline-none w-60 tracking-widest placeholder:tracking-normal" required>
          <input type="submit" value="Submit" class="px-6 py-2 bg-blue-500 text-white rounded-md hover:bg-blue-600 cursor-pointer">
        </form>
      </div>
    </div>
  </body>
</html>
